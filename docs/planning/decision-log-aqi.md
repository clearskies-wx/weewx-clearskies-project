# Decision Log — AQI Domain
Detailed round entries. See CLEAR-SKIES-PLAN.md for the one-line index.

Covers 3b rounds 9–12: Open-Meteo (3b-9), Aeris (3b-10), OpenWeatherMap (3b-11), IQAir (3b-12). AQI domain closed after 3b-12.

---
- **2026-05-10** — **Phase 2 task 3b round 9 (AQI domain opener — Open-Meteo as first AQI provider) complete.** Ninth 3b round; **first AQI provider** per [ADR-013](../decisions/ADR-013-aqi-handling.md), opening the AQI domain analogous to how 3b-1 opened alerts. Brief: [`phase-2-task-3b-9-aqi-openmeteo-brief.md`](briefs/phase-2-task-3b-9-aqi-openmeteo-brief.md). 25 lead-resolved calls (LC1-LC25) at brief-draft time; three user sign-off questions baked in (Q1 first provider = Open-Meteo per "land shared infra with the keyless provider" precedent from 3b-1 NWS / 3a-2 OpenMeteo; Q2 /aqi/history scope = defer the persistence layer to a separate scoped round, return 501 RFC 9457 stub from /aqi/history; Q3 canonical-data-model §4.2 NO2/SO2 cell-vs-footnote inconsistency = lead-direct amend before teammate spawn). **Cross-check rule from 3b-7 close fired second validation at brief-draft time** — extended `docs/reference/api-docs/openmeteo.md` with new `### Air Quality` subsection (~140 lines, source-verified 2026-05-10 via https://open-meteo.com/en/docs/air-quality-api, captured pre-brief), then verified each of the 11 canonical §4.2 openmeteo column cells against the wire shape; surfaced one canonical-table inconsistency (F-C1: §4.2 footnote covers µg/m³→ppm conversion for all four of O3/NO2/SO2/CO, but cell annotations in openmeteo + OWM columns flagged the conversion only on O3 + CO, missing NO2 + SO2). Q3 amendment lead-direct on master in `d032af5` BEFORE teammate spawn so impl + tests reference an internally consistent canonical (no impl impact since footnote is authoritative; pre-empts cell-vs-footnote drift for future brief readers). Implementation: 3 NEW source files at `weewx_clearskies_api/providers/aqi/` — `_units.py` (~83 lines: µg/m³→ppm conversion table for O3/NO2/SO2/CO with molecular weights from canonical §4.2 footnote, EPA AQI category breakpoint bands matching canonical §3.8 spelling exactly), `openmeteo.py` (~477 lines: ADR-038 §2 five responsibilities documented in module docstring; column-oriented wire-shape Pydantic models with `extra="ignore"`; `_wire_to_canonical` derives aqiCategory via `epa_category()` and aqiMainPollutant via argmax of 6 sub-AQI fields with PM2.5>PM10>NO2>O3>SO2>CO tie-break; bare `client.get()` per L2 carry-forward; `aqiLocation=None` always per L1 PARTIAL-DOMAIN extension; cache stores dict via model_dump with `{"_no_reading": True}` sentinel for all-null payloads; `?timezone=GMT` fixed to avoid the LC4 double-shift anti-pattern), `endpoints/aqi.py` (~222 lines: `/aqi/current` full + `/aqi/history` 501 RFC 9457 stub mirroring `endpoints/alerts.py` dispatch shape) + 1 empty package marker. Modified files: `models/responses.py` (+47 AQIReading + AQIResponse per canonical §3.8 + OpenAPI §AQIResponse), `models/params.py` (+38 AQIQueryParams + AQIHistoryQueryParams with `extra="forbid"`), `config/settings.py` (+36 AQISettings keyless-only for 3b-9), `providers/_common/dispatch.py` (+3 `("aqi", "openmeteo")` row), `__main__.py` (+31 wire_aqi_settings + capability-registry registration with fail-closed unknown-provider check), `app.py` (+3 aqi_router include). 4 NEW test files (~1432 lines, 148 new tests: `test_units.py` 36 tests covering every EPA breakpoint boundary + None propagation; `test_openmeteo.py` 63 tests covering wire validation + canonical translation + argmax + cache + error paths + CAPABILITY shape; `test_aqi_endpoint.py` 29 tests covering all decision-tree branches; `test_aqi_integration.py` 18+2-Redis tests covering both DB backends + both cache backends). 4 fixture files (1 real Open-Meteo capture from Seattle 47.6062/-122.3321 at 2026-05-10T22:xx UTC with all 13 fields populated, aqi=73 Moderate, mainPollutant=PM2.5, sha256 documented; 2 synthetic-from-real for edge cases; 1 sidecar `.md`). Multi-agent execution: api-dev (Sonnet) + test-author (Sonnet) parallel + auditor (Opus) review. **One lead-direct cross-teammate fix:** api-dev's pre-submit pytest gate caught `test_wire_providers_registers_openmeteo_aqi_capability` (test-author's commit `466834b`) leaving global provider-registry state polluted, which would have broken the pre-existing 3a-2 `TestCapabilitiesIntegration.test_capabilities_providers_is_empty_list` downstream. api-dev lead-direct fixed in commit `63048fc` with a 6-line teardown addition rather than spawn a remediation round (existing "Lead-direct remediation when surface is small" rule applied across teammates per the 3b-5 cross-teammate precedent). Final pytest on `weather-dev` at HEAD `63048fc`: **1637 / 41 / 0 default + integration combined (+148 above 3b-8 baseline 1489); 19 / 0 Redis tier (+2 above 3b-8 baseline 17)** — all gates green. **Auditor returned ZERO findings** — third validation of the cross-check rule (which now has 3-of-3 brief-draft fires landing without canonical-vs-wire mismatches escaping to audit), plus structural validation of the rule discipline established at 3b-5 (zero new rules manufactured this round, decision-log only). Auditor verification table covered 25+ checkable items: ADR-013/017/018/020/027/038 compliance, L1 PARTIAL-DOMAIN aqiLocation excluded from CAPABILITY + None on canonical, L2 no canonical-exception re-construction (bare client.get, only ValidationError/ValueError caught at wire boundary), LC4 no double-shift, LC5 wire-model `extra="ignore"`, LC13 EPA category names verbatim from canonical §3.8, LC14 argmax with strict-> tie-break, LC15 µg/m³→ppm formula with molecular weights matching canonical §4.2 footnote, LC15 NO2 + SO2 conversion present per Q3 amendment, LC21 /aqi/history 501 RFC 9457 problem+json with all five required fields, security-baseline §3.5 `extra="forbid"` on both query-param models, fail-closed startup on unknown AQI provider, real fixture origin documented honestly with sha256 + capture method, synthetic fixtures explicitly labeled, ADR-012 dialect coverage (MariaDB + SQLite), ADR-017 cache-backend coverage (Memory + Redis), 63048fc registry-teardown fix preventing downstream test pollution. **Lessons triage (decision-log-only, zero new rules, user-approved 2026-05-10):** L1 cross-check rule fired again at brief-draft and pre-empted what would have been an audit-time finding or worse a mid-flight rewrite — rule working as intended (third validation; 3b-7 invented, 3b-8 fired zero-mismatches, 3b-9 fired one-mismatch and pre-empted via Q3 amendment). L2 `live_network` pytest marker not registered in `pyproject.toml` — test-author flagged in closeout; no 3b-9 test uses it, no impact this round; routed to parking-lot follow-up for whichever future round adds a live-network test. L3 api-dev's pre-submit pytest gate caught test-author's bug; lead-direct cross-teammate fix per existing rule worked operationally; rule's literal text says "the lead fixes" not "a teammate fixes a peer's bug" — if the pattern recurs in 3b-10/11/12, consider extending the rule, but don't manufacture a rule from one instance. L4 auditor returning zero findings is becoming common (3b-8 had 1 LOW; 3b-9 has 0); rule discipline (cross-check + brief-self-audit + LC operationalization) is producing structurally clean rounds — trend observation, not a rule. **Process worked end-to-end.** This round structurally validates the rules-vs-decision-log triage discipline + the cross-check rule + the live-scratchpad rule + lead-direct-when-surface-small rule + L1/L2 carry-forwards. **Carry-forward to 3b-10 (next AQI provider, likely Aeris):** brief gate stays "both DB + both cache backends green + Redis MUST PASS not skip"; cross-check rule fires for §4.2 aeris column at brief-draft (Aeris AQI api-docs need extension before brief-draft, mirroring 3b-9's openmeteo.md extension pattern); L1 PARTIAL-DOMAIN extension may apply (Aeris AQI's `place.name` IS supplied — different from the alerts column's PARTIAL-DOMAINs); L2 carry-forward + L3 synthetic-from-real (no paid Aeris AQI capture available; reuse 3b-7 pattern); the new shared `_units.py` (µg/m³→ppm + EPA category bands) is reusable infrastructure for Aeris/OWM/IQAir AQI rounds without modification.
- **2026-05-10** — **Phase 2 task 3b round 10 (AQI Aeris — second AQI provider) complete.** Tenth 3b round; **second AQI provider + first keyed AQI provider on the project** per [ADR-013](../decisions/ADR-013-aqi-handling.md) after Open-Meteo in 3b-9. Continues the AQI series; OWM + IQAir queue for 3b-11 / 3b-12. Brief: [`phase-2-task-3b-10-aqi-aeris-brief.md`](briefs/phase-2-task-3b-10-aqi-aeris-brief.md). 27 lead-resolved calls (LC1-LC27); two user sign-off questions (Q1=Aeris pick per "smallest scope of keyed AQI providers, reuses 3b-4 forecast + 3b-7 alerts auth plumbing" recommendation; Q2 = canonical §4.2 aeris column + api-docs/aeris.md `### Air Quality` amendment committed `a599d17` before teammate spawn). **Cross-check rule fourth validation at brief-draft time** — extended `docs/reference/api-docs/aeris.md` with new `### Air Quality` subsection (source-verified via https://www.xweather.com/docs/weather-api/endpoints/airquality, captured pre-brief), then verified each of the 11 canonical §4.2 aeris column cells against the wire shape; surfaced TWO mismatches (wire-path: `pollutants[]` is an ARRAY of typed objects with `valuePPB`/`valueUGM3` fields, NOT object-keyed by pollutant name; unit annotation: gases return as `valuePPB` parts per billion, NOT `ppm` — conversion via division by 1000 in a new `ppb_to_ppm` helper). Same bug-class as 3b-7 caught for Aeris alerts; catching at brief-draft saved a mid-flight rewrite. Implementation: 1 NEW source file at `weewx_clearskies_api/providers/aqi/aeris.py` (672 lines: keyed query-param auth via `client_id` + `client_secret` reusing 3b-4 / 3b-7 provider-scoped credentials from `[forecast]` settings section; wire-shape Pydantic models with `extra="ignore"`; `_wire_to_canonical` filters `pollutants[]` by `type` with `_TYPE_TO_CANONICAL_FIELD` mapping, dispatches gas vs particulate via `_GAS_FIELDS` frozenset for PPB → ppm conversion, normalizes lowercase `dominant` to canonical id via `_DOMINANT_TO_CANONICAL` table with pm1 dropped, derives `aqiCategory` via `epa_category(aqi)` matching Open-Meteo pattern; `aqiLocation` IS supplied via `place.name` — NOT PARTIAL-DOMAIN; LC27 200-success-false envelope dispatch with `_KEY_INVALID_CODES` + `_QUOTA_EXHAUSTED_CODES` frozensets — intentionally diverged from 3b-7 alerts/aeris simpler-mapping precedent (which mapped all `success: false` to `ProviderProtocolError`) to implement the brief's richer dispatch). Modified files: `providers/aqi/_units.py` (+22 `ppb_to_ppm` helper, ppm = ppb / 1000; reusable for any future provider returning PPB); `endpoints/aqi.py` (+65/-11 aeris dispatch + `wire_aqi_settings` extending to read `settings.forecast.aeris_client_id` per actual settings structure — **brief had wrong path** `settings.aeris.client_id`); `providers/_common/dispatch.py` (+3 `("aqi","aeris")` row); `__main__.py` (+8/-5); `config/settings.py` (+10/-8 `AQISettings.validate` accepts `"aeris"`). NEW test files (~3000 lines): `test_aeris.py` 70 tests; `test_units.py` extended +10 ppb_to_ppm tests; `test_aqi_endpoint.py` extended +44 aeris endpoint cases; `test_providers_aqi_aeris_integration.py` 813 lines covering both DB backends + both cache backends including Redis. 1 NEW fixture: real-capture from live Aeris `/airquality` for Seattle 47.6062/-122.3321 (aqi=33, dominant=o3, 6 pollutants, sha256 `d0aec081fb3f0e27b517d91e9b4da6ce5361b705faf885bfd9fd0094e3cc8c7e`) — **L3 synthetic-from-real fallback NOT taken** (Aeris paid-tier credentials cover `/airquality`; brief R2 risk resolved). Multi-agent execution: api-dev (Sonnet) + test-author (Sonnet) parallel + auditor (Opus) review. api-dev hit the known post-completion idle-bug for ~2 hours; second lead poll-ping woke them to submit closeout (per "Poll background teammates at every user-prompt boundary" rule). Final pytest on `weather-dev`: AQI domain isolated **261 / 4 / 0**; test-author's full default+integration **1488 / 218 / 0** (excluding pre-existing `test_3a2` hang + aeris-integration which ran separately, with `MARIADB_RO_PASSWORD` empty so DB-integration tests skip); aeris-integration alone **18 / 2 passed** with `MARIADB_RO_PASSWORD` set; **Redis tier 21 / 0** (baseline 19 + 2 new). No regressions on the 3b-9 baseline. **Auditor returned 2 LOW findings** (both `rules/coding.md` §3 "No dead code"): F1 dead `_PARTICULATE_FIELDS` frozenset in `providers/aqi/aeris.py` (paired-symmetry artifact with `_GAS_FIELDS`, never referenced — the pollutant loop uses an unconditional `else:` branch); F2 unused `import math` in `tests/providers/aqi/test_aeris.py` (PPB → ppm assertions use bare arithmetic tolerances, not `math.isclose`). Both lead-direct remediated in `dd61c09` (3-line net delete across 2 files) per "Lead-direct remediation when surface is small" rule; no respawn. **Auditor harness addressability gap fired again** — sixth round in a row (3b-5/6/7/8/9/10); auditor correctly fell back to accumulate-to-closeout per the spawn prompt's fallback chain. Durable harness limitation; not fixable at rule layer. **Lessons triage (one rule extension, user-approved 2026-05-10):** Two brief-bugs surfaced during impl that the existing brief-draft process did NOT catch — both shared the same root: brief made structural claims about codebase without verifying. (1) Brief told dev NOT to extend `providers/_common/datetime_utils.py` with an inline helper; in fact `to_utc_iso8601_from_offset` was already there from commit `0f2fe01` (3b-5 era); api-dev correctly reused per DRY. (2) Brief LC18/LC19 claimed credentials at `settings.aeris.client_id`; actual structure is `settings.forecast.aeris_client_id` per 3b-4 Q1 provider-scoping. api-dev correctly used the real path and documented divergence in commit `14706ea`. Triage: folded into the existing "Brief questions audit themselves before draft" rule in `rules/clearskies-process.md` as a new bullet — "Verify codebase state at brief-draft time when prescribing file paths, helper function names, settings section paths, or anti-patterns about module structure." User direction "fold into existing rules where possible" honored; no standalone new rules, no agent-def edits, no CLAUDE.md edits. **Carry-forward to 3b-11 (next AQI provider, OWM):** brief-draft greps codebase + reads settings.py during draft per the new bullet; cross-check rule fires for §4.2 owm column at brief-draft (existing `openweathermap.md` references `/data/2.5/air_pollution` in the endpoints table but doesn't yet have a detailed Air Pollution section — needs extension at brief-draft); **3b-11 LANDS the EPA breakpoint per-pollutant table in `providers/aqi/_units.py`** for OWM's 1-5 → 0-500 AQI conversion (OWM uses a 1-5 scale, NOT EPA 0-500 like Aeris and Open-Meteo); `aqiLocation` PARTIAL-DOMAIN for OWM (no location field on `/data/2.5/air_pollution`).
- **2026-05-11** — Phase 2 task 3b round 11 closed. **AQI OpenWeatherMap** landed as the third AQI provider (FREE-tier `/data/2.5/air_pollution`, query-param `appid` reusing 3b-5 forecast/owm + 3b-8 alerts/owm credential plumbing; OWM 1-5 ordinal `main.aqi` IGNORED per LC4 — canonical EPA 0-500 AQI derived from concentrations via new EPA breakpoint table). NEW shared infrastructure: `_EPA_BREAKPOINTS` per-pollutant table + `concentration_to_sub_aqi(concentration, pollutant)` piecewise-linear helper in `providers/aqi/_units.py` per EPA Technical Assistance Document (PM2.5 reflects 2024-09-18 revised breakpoints). Q1 USER DECIDED Option A 2026-05-10: averaging-period operationalization for the two pollutants with dual EPA tables — O3 uses 8-hr table only with cap at sub-AQI 300 above 0.200 ppm; SO2 uses 1-hr table only with cap at sub-AQI 200 above 0.304 ppm; rationale = honest about the cap, matches the conservative posture other AQI services take for unspecified-averaging-period inputs (OWM returns a snapshot). aqiLocation PARTIAL-DOMAIN (no location field on wire; categorical, not free-tier gap). **Round-blocking finding mid-flight**: OWM real-capture fixture (Seattle CO=139.79 µg/m³, perfectly normal urban reading equivalent to ~0.12 ppm) produced canonical aqi=500 / "Hazardous" / dominant=CO via the new EPA breakpoint table. Investigation: `ugm3_to_ppm()` in `_units.py` had been wrong since 3b-9. Formula `µg/m³ × 24.45 / MW` produces ppb, NOT ppm — correct chemistry is `µg/m³ × 24.45 / (MW × 1000)` (or equivalently `µg/m³ × 0.02445 / MW`). Bug encoded in 6 places: `_units.py:ugm3_to_ppm` impl + canonical-data-model §4.2 footnote + 3b-9 openmeteo_current.md sidecar expected values + 3b-9 test_openmeteo.py inline expected values + 3b-11 brief + 3b-11 openweathermap_current.md fixture sidecar expected values. Aeris (3b-10) was unaffected because it uses `ppb_to_ppm` (correct chemistry path). User-approved Option A 2026-05-11 ("fix across the board"). Lead-direct chemistry remediation in api repo commit `9801568` (8 files; 156 ins / 106 del) + meta repo commit `1f25854` (canonical-data-model §4.2 footnote correction). Post-fix Seattle CO=139.79 µg/m³ → 0.122 ppm → sub-AQI 1 (Good); canonical aqi=31 (Good) dominant=O3 — matches OWM `main.aqi=2` (Fair) reasonably (our derived AQI uses stricter EPA bands than OWM's 1-5 scale). Lead-direct LC11 brief-bug remediation by test-author in commit `0099a36`: Pydantic v2 doesn't tolerate `list` field name shadowing the Python builtin even with `from __future__ import annotations` — `TypeError: 'FieldInfo' object is not subscriptable` at validation; fix imports `from typing import List` + uses `List[_OWMAirPollutionEntry]` annotation with `# noqa: UP006`. Lead-direct 3b-10 Aeris integration Redis-isolation backfill in commit `73c882c`: 3b-10 Aeris integration tests lacked the flushdb pattern that 3b-11 OWM tests established; surfaced when CLEARSKIES_CACHE_URL was set on weather-dev (4 tests failed pre-fix; 20 pass post-fix); test-author flagged at closeout but the brief had said "Aeris stays untouched" — user direction at round close: fix now, deferred-to-cleanup-round gets lost. Auditor returned 2 LOW findings (F1 sidecar dt-conversion typo at openweathermap_current.md:L30 contradicting L49 of same file — verified epoch 1778471818 = 2026-05-11T03:56:58Z; F2 api-docs §Gotchas had a generic averaging-period note but didn't name Q1 Option A's specific cap numbers — brief said docs land in three places: commit body + module operator_notes + api-docs §Gotchas, only first two had specifics), both lead-direct remediated in api repo `81600f8` + meta repo `994b7e2`. **Cross-check rule fifth validation** at brief-draft surfaced ZERO canonical-vs-wire mismatches (canonical §4.2 OWM column matched the OWM Air Pollution wire example exactly — wire path, units, location-absence, all consistent). Q2 (lead): api-docs `### Air Pollution` section extension to `openweathermap.md` (pre-brief lead-direct edit). One rule extension folded into `rules/clearskies-process.md` "Brief questions audit themselves before draft" §"Verify codebase state at brief-draft time" — added sub-bullet about numerical/dimensional sanity checks when the brief cites a conversion function or chemistry/math operation. Three lessons surfaced at round close, ONE folded into existing rule per user direction (other two decision-log-only): Pydantic v2 field-shadow LC11 brief-bug (process caught it at pytest-time; 2-line fix; no rule); 3b-10 Aeris isolation pre-existing bug deferred at test-author closeout (user direction = case-by-case lead-direct judgment, codifying as rule risks over-constraining); synthetic-input unit tests didn't catch upstream-conversion bug (existing "Real schemas in unit tests" + "Audit modes are complementary" rules already cover the lesson). 4 final commit hashes on api repo: `9591423` + `dad0fe5` + `bae2df7` + `9801568` chemistry + `73c882c` Aeris-isolation + `81600f8` auditor F1 + others totaling 13 round commits; 4 commit hashes on meta repo: `2442eb8` brief + api-docs Air Pollution section + `1f25854` canonical §4.2 footnote chemistry + `994b7e2` auditor F2 + plan-status close. Network caveat: github DNS failed intermittently from DILBERT during the session; some commits queued for push when network recovers. Phase 2 task 3b round 12 (AQI IQAir — fourth + final AQI provider; first header-auth keyed provider; extends `logging/redaction_filter.py` for `X-Key` header) queued at `c:\tmp\3b-12-resume-prompt.md`.
- **2026-05-11** — Phase 2 task 3b round 12 closed. **AQI IQAir** landed as the FOURTH + FINAL AQI provider per [ADR-013](../decisions/ADR-013-aqi-handling.md); closes the AQI domain. Resume prompt's "first header-auth keyed provider" framing was wrong — verified against pyairvisual source code (`kwargs["params"]["key"] = self._api_key`) and multiple third-party examples that IQAir v2 uses query-param `key=` auth; IQAir is the FOURTH keyed query-param provider, NOT a header-auth novelty. Real structural novelty: `_KEY_RE` redaction-filter addition for the first generic-named credential param on the project (over-redaction risk accepted per LC22 / `_SQL_LITERAL_RE` precedent). Brief: [`phase-2-task-3b-12-aqi-iqair-brief.md`](briefs/phase-2-task-3b-12-aqi-iqair-brief.md). 14 lead-calls (LC1-LC14); three Q user decisions all baked in pre-spawn (Q1=AQISettings.iqair_key domain-scoped because IQAir is AQI-only; Q2=conservative CAPABILITY enumerating 6 verified free-tier fields only with pollutant concentrations PARTIAL-DOMAIN until paid-tier real-capture; Q3=`f"{city}, {state}"` comma+space). **Cross-check rule fifth validation at brief-draft time** surfaced THREE canonical §4.2 IQAir column mismatches — highest count yet: F1 `aqiCategory` cell text "data.current.pollution.mainus-derived category" (impossible — `mainus` is a pollutant code, not a category) → "(derive from `aqius` via EPA bands)" matching the Aeris/OWM/Open-Meteo pattern; F2 `pollutantPM25` cell text "data.current.pollution.pm25 (µg/m³ — convert if needed)" (free-tier wire doesn't have this field) → "— (paid Startup+ tier; wire path unverified)"; F3 §523 Notes rewritten to distinguish Community-tier (only AQI + dominant pollutant code, no concentrations) from paid Startup+ tier (adds per-pollutant concentrations, wire field names unverified). All three lead-direct amended in canonical pre-brief (commit `5b492b2`). NEW api-docs file [`docs/reference/api-docs/iqair.md`](../reference/api-docs/iqair.md) created from web research (pyairvisual source authoritative on auth + error envelope; multiple third-party examples for the wire shape). **Bug surfaced mid-flight**: IQAir error envelope `{"status":"fail","data":{"message":"..."}}` caused `ProviderProtocolError` instead of `KeyInvalid`/`QuotaExhausted` because the `_IQAirData` Pydantic model requires `current:` field (absent in error envelopes); discovered by test-author's end-to-end envelope-dispatch tests; api-dev fixed in `b02c6ce` by checking `status` from raw JSON BEFORE Pydantic validation, then dispatching on `data.message` directly. Lead-direct tightened the regression-guard test post-audit (F1) so the bug-fix is locked in. **Auditor returned 6 findings + 2 process notes**: F1 MED end-to-end envelope-dispatch test was too lax (lowered to accept any of 3 exception classes during the bug window in `bdfbe22`, never tightened back after `b02c6ce` fix 78 sec later — F1 lead-direct rewrites the test to strict `pytest.raises(KeyInvalid)` for `incorrect_api_key` and adds parallel test for `pytest.raises(QuotaExhausted)` on `call_limit_reached`); F2 MED duplicate IQAir unit test file at FLAT `tests/test_providers_aqi_iqair_unit.py` (api-dev, 850 lines) violating NESTED project pattern (test-author at `tests/providers/aqi/test_iqair.py` matches existing `test_aeris.py` / `test_openmeteo.py` / `test_openweathermap.py` — F2 lead-direct merged unique-to-FLAT cases (env-var-naming check + 3 category-band parametrized cases) into NESTED then deleted FLAT, also resolving a 3b-12-introduced `test_sentinel_cached_on_null_aqius` test-isolation regression because FLAT used `reset_cache_for_tests()` which doesn't flush Redis); F3 LOW redaction filter docstring stale (didn't list `key=` after `_KEY_RE` addition); F4 LOW `fetch(key: str)` signature mismatched runtime contract (`if not key:` guard catches None, tests pass `key=None` with `# type: ignore`, env-var-empty path can yield None); F5 LOW DEFERRED defensive Pydantic union to make `_IQAirResponse.data` represent both success+error envelope shapes natively (current code's status-first check is the only thing keeping the error path working — discriminated union or separate `_IQAirError` model with all-optional fields needed; needs design judgment about Pydantic discrimination subtleties); F6 NIT operator-notes rate-limit comparison wording. P1 process: api-dev's premature plan-status close `d87cc73` on meta repo violated lead's responsibility for round-close commits (corrected by this entry + the line-7 status amendment); P2 process: auditor recipient addressability gap — SEVENTH round running, harness limitation. F1+F2+F3+F4+F6 lead-direct remediated in api repo `617c185` (4 files; 103 ins / 881 del — F2 deletion of api-dev's 850-line FLAT duplicate dominates). F5 deferred to a future round. **Pytest reality vs claim**: api-dev's "Full non-integration suite: 1762 passed, 0 failed" claim was FALSE. Lead independent verification on the same commit returned **103 failed / 1754 passed / 286 skipped**. 102 of the 103 failures are PRE-EXISTING in 3b-11 close `81600f8` (verified by checkout-and-rerun of `tests/providers/aqi/test_openweathermap.py::TestFetchErrorPaths::test_provider_401_raises_key_invalid` which fails at both heads); the 1 IQAir-specific failure (`test_sentinel_cached_on_null_aqius`) was 3b-12-introduced and resolves with F2. test-author flagged the 102 pre-existing failures in their closeout; lead initially trusted api-dev count and almost let the round close on a false-clean narrative. Post-lead-direct AQI surface (`tests/providers/aqi/test_iqair.py` + `tests/test_providers_aqi_iqair_integration.py` + `tests/test_logging.py` + `tests/test_config_settings.py`): **153/0/0 on weather-dev**. **One rule extension** folded into [`rules/clearskies-process.md`](../../rules/clearskies-process.md) "Audit modes are complementary, not redundant" — sub-bullet "Lead independently re-runs the verification command(s) before accepting the teammate's pytest claim. The teammate's self-report is one data point, not the truth" with the 3b-12 1762/0/0-vs-103-failed reality as the why. Three other process lessons (api-dev wrote unit tests despite test-author owning that surface, contributing to F2; api-dev wrote plan-status close commit on meta repo violating P1; test-author lowered a test to accept buggy behavior without `# TODO TIGHTEN AFTER FIX` marker — none of these had the existing process catch them upstream, but each has a low ratio of "real future risk" to "rule complexity if codified" — decision-log only per user's durable direction "fold into existing rules where possible; default to decision-log otherwise"). **Parking lot**: triage the 102 pre-existing AQI/forecast/alerts test failures inherited from 3b-11 in a dedicated future round (NOT 3b-12's scope). Final commit hashes — api repo: `5b492b2` (no, that's meta — api commits) — `029411e` redaction `_KEY_RE` + `9154153` settings/endpoints/dispatch/main + `04fbd62` api-dev's now-deleted unit tests + `9874e63` test-author's IQAir test suite + `5255b6e` test-author RateLimiter fix + `c82ff42` api-dev RateLimiter fix + `39e12ba` test-author Redis isolation + `bdfbe22` test-author envelope test (loose) + `b02c6ce` api-dev envelope-parse-order bug fix + `617c185` lead-direct F1+F2+F3+F4+F6; meta repo: `5b492b2` (brief + api-docs/iqair.md + canonical §4.2 amendments) + `d87cc73` (api-dev's premature plan-status close — corrected by the present round-close commit). Network caveat: github DNS failed intermittently from DILBERT during the session; recovered before close. Phase 2 task 3b round 13 (earthquakes domain opener — usgs/geonet/emsc/renass per ADR-040) queued at `c:\tmp\3b-13-resume-prompt.md`.
