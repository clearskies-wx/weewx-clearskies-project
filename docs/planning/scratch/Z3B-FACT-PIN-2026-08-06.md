# Z3B FACT-PIN (condensed) — dashboard ad2ecf9 — 2026-08-06

Full agent report in session transcript. Load-bearing pins:

## Layer stack (HeatMapCard.tsx, one shared SVG space)
imagery mosaic (:1154-1177, north-up lon/lat stretch via lonToScreenX/latToScreenY :344-345,
NO transform exists) → row separators → colour cells (data frame: X=cross-shore distance,
Y=TRANSECT INDEX, not metric) → zone overlays (:939-997) → structure hatching → breaker
glyphs → BD-7 gutter → chrome. Card's own comment :176-197 confirms the north-up/unrotated
mismatch nearly verbatim.

## Imagery source
Runtime tile fetch (naip|esri) via GET /api/v1/imagery/config (types.ts:2256-2276): returns
tileUrl TEMPLATE + attribution + (NAIP-only) coarse CONUS bounds. NO corner coords, NO EPSG —
georegistration is client-side slippy-tile math (implicit EPSG:3857, HeatMapCard :234-249).
Only spatial datum client-side = single spot center (spotLat/Lon).

## Z-D4 PREMISE CONTRADICTIONS (design cannot be built as written)
1. The two control points (segment start/end) exist in NEITHER frame client-side — only one
   center point is plumbed; "the existing segment geometry" is not present in the dashboard.
2. Data-frame Y axis has NO metric along-shore geometry (index only, card's own comment
   :192-193) — the 1% segment-length gate row is not computable from today's data.
   (distanceFromCenter exists in types.ts:1922 for a DIFFERENT type; not on heat-map rows.)
3. Transects are a RADIAL FAN around one origin (:182-186), not a linear segment —
   "segment start/end" framing mismatches the geometric model.
4. No imagery georeference from the API — "via the imagery's own georeference" premise false.
COULD-NOT-VERIFY: whether the marine API could expose segment/transect-origin geometry
(server-side, out of dashboard pin's scope). Per-row transectBearingDeg EXISTS on the wire.

## Z-D3 verify target pin
Card reads ONLY the singular aggregate surfZones (:939-976); NEVER reads perBreakZones (the
authoritative per-break array added 2026-08-05, types.ts:2060) — multi-break rendering today
= fixed-width heuristic bands from breakPoints via splitBreakPoints (:501-508, :978-997),
NOT the real per-break impact/foam extents. Z3's "expected: no code change" premise is
therefore DOUBTFUL — likely promotes to a fix task (consume perBreakZones).
