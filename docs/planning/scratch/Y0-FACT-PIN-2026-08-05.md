# Y0 Fact-Pin — Boundary spectrum integrity anchor table

**Repo pinned against:** `weewx-clearskies-marine` @ `bdf4db8314db359c6e18b22a900a6f4e37dcb620`
(2026-08-05 14:17:07 -0700), working tree clean. All file:line citations below are against this
commit unless noted.

---

## (a) PRIORITY SUB-QUESTION VERDICT

**The real-spectrum boundary path is NOT dead and NOT unwired. It is the live, sole production
path for the L1 boundary, has been since 2026-07-26, and is confirmed fetching on librewxr
tonight (2026-08-05/06).**

- `ww3_to_swan_boundary()` — the function the plan's Y-D4 names as "the collapse site" — **does
  not exist in this codebase.** `grep -rn "def ww3_to_swan_boundary"` and `grep -rn
  "ww3_to_swan_boundary"` (whole repo) return zero hits for the function definition; the only
  hits are historical comments describing its deletion (`swan_formats.py:2297-2317`).
- It was deleted in commit `5fe77af` (2026-07-26 09:21:12 -0700, "feat(T8.10c round2/T8.10f):
  real multi-station spectral L1 boundary, no uniform fallback"), preceded by `a671390`
  (2026-07-26 08:24:23, "feat(T8.10c/T8.10d): real WW3 spectral boundary writer + CGRID low-freq
  widen"). Both predate the plan by 10 days.
- The **replacement is fully wired**, not behind a flag:
  - `ww3_spectrum_to_swan_boundary()` (`swan_formats.py:2369-2508`) writes a real SWAN 2-D
    spectral boundary file (SWAN Appendix D ASCII format: `AFREQ`/`NDIR`/`QUANT`/`VaDens`
    matrix) from parsed WW3 station spectra — no synthesized shape, no `DSPR` constant anywhere
    (asserted in its own docstring and confirmed by reading the body).
  - `ww3_boundary_files_and_command()` (`swan_formats.py:2538-2666`) assembles one file per
    selected station and one `BOUNDSPEC SIDE <s> CCW VARIABLE FILE <len> '<file>' ...` command
    line per side (multi-station, per-side).
  - `swan_runner.py:4752-4759` calls it for the L1 ("outer") run, unconditionally, every cycle
    that reaches this point.
  - `providers/nearshore/swan.py:2637-2723` is the top of the call chain: it calls
    `select_boundary_stations_with_cycle_fallback()` and, on ANY failure
    (`BoundaryNotViableError` or otherwise), **raises rather than substituting** — logs
    `"no-publish: ww3_boundary_failed"`, calls `state.record_no_publish(...)`, and re-raises so
    the whole SWAN cycle aborts (last-good forecast preserved). There is no code path left
    anywhere in this call chain that fabricates a boundary.
- **Live evidence, librewxr, tonight** (`journalctl -u weewx-clearskies-marine`, last 24h, 4
  separate cycles 23:49–00:10 UTC 2026-08-05/06): `ww3_station_selection` logs `"L1 centre
  (33.5854, -117.9781) routes to WW3 product 'ocean'"`, followed by four real HTTP 200 GETs to
  `nomads.ncep.noaa.gov/.../gfswave.{46222,46223,46253,46256}.spec`, then `"select_boundary_
  stations: ocean -- bearing 215.0 deg -> sides S/W -- S side: [('46223', ...)], W side:
  [('46256', ...), ('46222', ...), ('46253', ...)], rejected: 4032"`. This repeats every cycle
  (~7 min apart in the sampled window) — this is not a one-off fetch, it is the steady-state
  behavior.

**What does NOT exist that Y-D1 assumes:** there is **no tiered fallback**. Y-D1 designs Tier
1 (full spectrum) / Tier 2 (per-partition JONSWAP+cosᵐ superposition) / Tier 3 (single-peak,
flagged degraded). Current code is **single-tier**: real multi-station spectrum, or hard abort.
`grep -rn "boundary_degraded"` (whole repo): zero hits. `grep -rn "class
BoundaryNotViableError|raise BoundaryNotViableError"` (`ww3_station_selection.py`): the only
"fallback" is cross-*cycle* (older WW3 model cycle, same real-spectrum mechanism — see §(d)),
never a degraded/synthesized spectrum. So:
  - **Y-D1 Tier 1 is effectively already implemented** for the L1 WW3→SWAN boundary (multi-
    station real 2-D spectra, `BOUNDSPEC ... VARIABLE FILE`). This may let Y1 skip re-building
    Tier 1 and focus on: (i) verifying/tightening it against Y-D2's 2% conservation invariant,
    (ii) building Y-D1 Tiers 2/3 as new code (they do not exist), (iii) reconciling why "today's
    value 0.47–0.51 m" (the plan's stated "before" record, presumably measured 2026-08-05 —
    AFTER this path went live) is still low — see the caveat below.
  - **Y-D3 "no re-tuning downstream" and Y-D4's premise both need a look**, because the plan's
    root cause #2 narrative ("Our handoff synthesizes ONE parametric peak from scalar WW3
    fields" — plan line 30) describes the PRE-T8.10c state (superseded by concern **C-87**,
    `docs/planning/briefs/WW3-SPECTRAL-BOUNDARY-DATA-BRIEF.md`), not bdf4db8.

**Caveat — this does not mean the "⅓ energy" symptom is explained or resolved; it means the
WW3→SWAN L1 boundary specifically is not its likely source.** Live journal evidence (same
window, `surf_1d_pipeline`) shows a DIFFERENT, downstream collapse point still active tonight:
repeated `WARNING`s of the shape *"transect N has NO per-transect PT* swell partitions at its
own handoff point this hour — bulk-falling-back for THIS transect only (Hs=0.43 m, Tp=6.9 s,
Dir=220°)"* (`surf_1d_pipeline.py:1577-1589`, message template at line ~1580-1587) — i.e. the
SWAN L3→1D handoff, not the WW3→SWAN L1 boundary, is still observed collapsing to one bulk
scalar triple per transect when SWAN's own partitioning finds nothing at that point. This is
consistent with `swan-commands-extract.md`'s own measured correction (1 partition in 95.2% of
rows, `TABLE_1.txt`, 2026-07-25). **I am not able to determine, from static reading, whether this
downstream collapse is (i) a genuine remaining code defect Y1 should also address, (ii) the
correct physical outcome of feeding SWAN a real boundary that still resolves to one dominant
partition at this nearshore point, or (iii) in-scope for X/Z rather than Y.** Flagging as a
finding for the coordinator to route — not reinterpreting Y-D1's design myself per this task's
scope limits.

---

## (b) Boundary construction path

| Item | File:line | Detail |
|---|---|---|
| `ww3_to_swan_boundary()` (plan's named collapse site) | **does not exist** | Deleted in `5fe77af` (2026-07-26). Only historical references remain, `swan_formats.py:2297-2317` (comment block explaining the deletion). |
| `ww3_spectrum_to_swan_boundary()` (current real writer) | `swan_formats.py:2369-2508` | Consumes: `list[WW3Spectrum]` (parsed WW3 `.spec` station files, one per forecast timestep, same station/product/cycle — `ww3_spectrum.py`'s `WW3Spectrum` dataclass, `freqs_hz`, `dirs_rad`, `energy[i_dir][i_freq]`, `header.lon/lat/station_id/depth_m`). Writes: one SWAN 2-D spectrum file per station (SWAN Appendix D ASCII: `TIME`/`LONLAT`/`AFREQ`/`NDIR`/`QUANT`/`VaDens` blocks, `swan_formats.py:2467-2508`). Applies two conversions, both cited at module level (`swan_formats.py:2319-2366`): WW3 nautical "going-to" radians → SWAN nautical "coming-from" degrees (`_ww3_dir_rad_going_to_to_swan_nautical_coming_from_deg`, line 2349-2355), and WW3 m²/Hz/rad → SWAN m²/Hz/deg (`_RAD_TO_DEG_DENSITY_FACTOR = π/180`, line 2366). Direction axis is sorted ascending before emission (SWAN requires monotonic `NDIR`) — `swan_formats.py:2448-2465`. |
| `ww3_boundary_files_and_command()` (assembles the SWAN command) | `swan_formats.py:2538-2666` | Consumes: `BoundaryStationSelection` (from `ww3_station_selection.select_boundary_stations()`/`..._with_cycle_fallback()`) + `utm_zone` + L1 box (`lat_min/lat_max/lon_min/lon_max`). Writes: `{filename: file_content}` dict (one 2-D spectrum file per selected station, via the function above) + one `BOUNDSPEC SIDE <s> CCW VARIABLE FILE <len_m1> '<f1>' 1 <len_m2> '<f2>' 1 ...` line per populated side (`swan_formats.py:2645-2665`). `[len]` computed as UTM meters from the side's CCW begin corner to each station's foot-point (R5 fix, `swan_formats.py:2568-2586` docstring + 2651-2664 body) — required because L1 is Cartesian/UTM projected, not spherical. |
| Call site 1 (L1/"outer" build) | `swan_runner.py:4752-4759` | Only path that calls `ww3_boundary_files_and_command()`; runs unconditionally for `grid_level == "outer"` when `reused_boundary_command_lines` is `None` (i.e. every normal nonstationary cycle). Writes each returned file to `run_dir` at `swan_runner.py:4760-4761`. |
| Call site 2 (E8 stationary-fill reuse) | `swan_runner.py:4218-4262`, `4739-4745` | `_reused_l1_boundary_command_lines()` reads back the LAST full run's already-written L1 `BOUNDSPEC` lines verbatim from disk (`swan_runner.py:4218-4262`) for the hourly stationary "fill" run — no new fetch, no synthesis; if set, `swan_runner.py:4741-4745` skips the builder entirely. |
| Top-of-chain caller (fetch + selection, before any file-writing) | `providers/nearshore/swan.py:2599-2723` | Calls `select_boundary_stations_with_cycle_fallback()` (line 2637-2654); on success stores `ww3_boundary` (a `BoundaryStationSelection`) and threads it into `swan_runner.run(...)` (used at `swan_runner.py:3283`); on ANY exception, raises after logging + `state.record_no_publish("ww3_boundary_failed", ...)` (lines 2656-2723) — no substitute boundary is ever built. |

---

## (c) Current SWAN boundary command per level + doc-code drift

| Level | Boundary command actually emitted | Code that emits it | Source of the boundary data |
|---|---|---|---|
| **L1** ("outer") | `BOUNDSPEC SIDE <N/E/S/W> CCW VARIABLE FILE <len> '<file>' 1 ...` (one line per side that has stations) | `swan_formats.py:1739-1744` (emission point inside `build_swan_input`); command text built by `swan_formats.py:2645-2665` | Real multi-station 2-D WW3 spectra, §(b) above. Also writes `NESTOUT 'inner' '<nest_out_file>' OUTPUT ...` (`swan_formats.py:2013-2016`) for L2 to consume. |
| **L2** | Built first as "outer" (would emit `BOUNDSPEC SIDE` dead-scaffolding lines per the L1 logic, since `ww3_boundary_files_and_command()` requires a non-empty selection and has no calm fallback — `swan_runner.py:3173-3184` explains this is intentional, reusing L1's already-fetched selection just to satisfy the non-empty contract), **then those lines are stripped by a post-write patch and replaced with `BOUNDNEST1 NEST '<L1 nest_out file>' CLOSED`** (`swan_runner.py:3202-3221`, patch loop at 3205-3221). The BOUNDSPEC L2 would have written is therefore never read by SWAN. L2 also writes its own `NESTOUT` for L3 (same emission code, `swan_formats.py:2013-2016`, since L2 is also run with `grid_level="outer"`). | `swan_runner.py:3185-3221` | L1's own boundary spectrum, propagated internally by SWAN via `BOUNDNEST1`/`NESTOUT` (SWAN-internal nesting, no WW3 fetch at this level). |
| **L3** (and L4 structure grid, when present) | `BOUNDNEST1 NEST '<parent nest_in file>' CLOSED` — emitted directly, no patch needed, because these are built with `grid_level="inner"` from the start (`swan_formats.py:1745-1752`) | `swan_runner.py:3917-3993` (L3 clusters), `swan_runner.py:4126-4127` (L4) both call `_write_input_files(..., "inner")` with `ww3_boundary={}` (ignored for inner — `swan_runner.py:4463`) | Parent level's (L2, or L3 for L4) `NESTOUT` file, copied to `nest_in.dat` between runs (`swan_runner.py:3140-3147` for the L1→L2 copy; analogous copy exists per-level down the chain). |

**Cross-check against `docs/reference/swan-commands-extract.md`:**
- The doc's "3-level nesting file flow" section (lines 282-294) correctly describes the
  `NESTOUT → BOUNDNEST1` chain for L2/L3 — **no drift there.**
- **Drift found:** the doc has **zero mentions** of `BOUNDSPEC`, `TPAR`, `ww3_spectrum`, or
  `ww3_station_selection` anywhere (`grep -n "BOUNDSPEC\|TPAR\|ww3_spectrum\|ww3_station_
  selection" docs/reference/swan-commands-extract.md` → no output). The L1 boundary command —
  the actual live production command since 2026-07-26 — is **entirely undocumented** in this
  reference file. The "Per-level physics summary" table at the doc's end (lines 504-516) lists
  GEN3/BREAKING/FRICTION/TRIAD/SETUP/DIFFRACTION/NUMERIC/OBSTACLE per level but has no boundary
  command row at all. This is a genuine doc-code sync gap Y3 should close (PROVIDER-MANUAL +
  ARCHITECTURE + swan-commands-extract per the Y3 task row already calls this out).

---

## (d) `ww3_spectrum` provider

| Item | File:line | Detail |
|---|---|---|
| Module | `weewx_clearskies_marine/services/ww3_spectrum.py` | Not a dispatch-registered provider (same placement class as `bathymetry_resolver.py`) — feeds a model boundary condition, not a canonical `MarineForecastPoint` field (module docstring, lines 1-13). |
| Products fetched | `ww3_spectrum.py:176-199` | Two NOAA NOMADS products, same parser: **ocean** `gfswave.<STATION>.spec` — `.../gfs.prod/gfs.{YYYYMMDD}/{CC}/wave/station/bulls.t{CC}z/gfswave.{station_id}.spec` (line 176-187), 6-hourly cycles (00/06/12/18Z), 50 freq × 36 dir (module docstring line 16, self-describing header, not hardcoded — parsed at `ww3_spectrum.py:355-356`). **great_lakes** `glwu.<STATION>.spec` — `.../glwu.prod/glwu.{YYYYMMDD}/bulls.t{CC}z/glwu.{station_id}.spec` (line 190-199), hourly cycles, 32 freq × 36 dir. Per-station files only, never the multi-GB tarballs (docstring lines 26-30). |
| Fetch function | `fetch_station_spectrum()`, `ww3_spectrum.py:202-240` | Rate-limited 2 req/s against `nomads.ncep.noaa.gov` (`_rate_limiter`, line 238; module docstring 111-116 flags a known double-limiter risk against T8.10a's separate NOMADS client, unresolved, out of this task's scope). |
| Parser | `parse_ww3_spectrum()`, `ww3_spectrum.py:318+` | Raises `WW3SpectrumParseError` on any truncation/malformity — never returns a partial spectrum (docstring lines 93-101). Direction-major/frequency-minor energy matrix layout (verified against live `.bull` Hs, 1-3% reconciliation — docstring lines 32-52). Circular-aware direction bin-width integration (docstring lines 54-68). No GRIB-style missing-value sentinel in this ASCII format (docstring lines 80-91). |
| Product routing (which product for which boundary) | `product_for_extent()`, `ww3_station_selection.py:1404-1420` | Classifies the L1 extent's CENTRE via `enrichment.bathymetry.classify_region()` — Great Lakes region → `"great_lakes"`, else `"ocean"`. Live tonight: HB (33.5854, -117.9781) → `"ocean"` (journal, §below). |
| Spatial coverage / station selection | `select_boundary_stations()` / `..._with_cycle_fallback()`, `ww3_station_selection.py:1136-1296` / `1429-1539`+ | Distance-limited to **one native WW3 grid cell per product**: ocean 18.5 km (docstring + `_max_distance_km`, line 378-383), Great Lakes 2.5 km. Requires ≥2 total stations across exactly 2 offshore sides (derived from the geography ray-cast open-water bearing, not beach facing — `swan.py:2644-2653`), ≥1 per side, else raises `BoundaryNotViableError` (`ww3_station_selection.py:1259-1261` for the total<2 check; class at line 559). Depth/kd-agreement criteria also applied (`_finalize_stage2`, referenced throughout; not fully traced — see "could not determine" below). |
| Temporal coverage / cadence | `ww3_station_selection.py:1396-1401`, `1443-1444` | Ocean: 6-hourly cycles. Great Lakes: hourly. `fetch_station_spectrum()` itself takes an explicit `cycle` param and makes no cadence assumption (`ww3_spectrum.py:104-109`) — cadence and cross-cycle logic live entirely in `ww3_station_selection.py`. |
| Staleness / cross-cycle fallback | `select_boundary_stations_with_cycle_fallback()`, `ww3_station_selection.py:1429-1539` | Anchors to the SWAN run's own HRRR wind cycle time (`providers/nearshore/swan.py:2628-2635`, `_hrrr_dt`), rounds down to the nearest published cycle hour for the routed product, then retries up to `_MAX_CYCLE_FALLBACKS = 3` (line 1426) older cycles if the newest is not yet published — matching the PROVIDER-MANUAL's documented `wavewatch.fetch()` cadence-fallback convention reused here (comment, lines 1422-1425). One aggregated WARNING logged only when the newest (target) cycle fails and it falls back (lines 1511-1529), not per-station. |
| Disk cache | **None found in this module.** | `ww3_spectrum.py` has no caching layer of its own — every call is a live HTTP fetch. The only "reuse" is E8's stationary-fill run reading back the SAME already-fetched cycle's already-written boundary files from the just-completed nonstationary run's own `run_dir` (`swan_runner.py:4218-4262`), not a general TTL cache. |
| On total failure | `providers/nearshore/swan.py:2656-2723` | No boundary → **entire SWAN cycle aborts** (`raise` at the end of the except block, line 2723). No degraded/calm/synthesized substitute at any point in this chain (explicit design intent, comment block 2656-2696, citing operator ruling C-76 and T8.10c round 2/T8.10f). Last-good forecast cache preserved; runner loop retries the SAME cycle every 5 min rather than advancing (comment, lines 2676-2683). |

**Live journal evidence, librewxr** (`sudo journalctl -u weewx-clearskies-marine --since '24 hours ago' --no-pager`, run read-only 2026-08-06 ~00:1x UTC), 4 cycles sampled (23:49, 23:55, 00:02, 00:10 UTC):

```
weewx_clearskies_marine.services.ww3_station_selection: select_boundary_stations: L1 centre
  (33.5854, -117.9781) routes to WW3 product 'ocean' (6-hourly cycles, max station distance 18.5 km)
httpx: HTTP Request: GET https://nomads.ncep.noaa.gov/pub/data/nccf/com/gfs/prod/gfs.20260805/18/
  wave/station/bulls.t18z/gfswave.46222.spec "HTTP/1.1 200 OK"
weewx_clearskies_marine.providers._common.http: Provider HTTP 200 https://nomads.ncep.noaa.gov/.../gfswave.46222.spec
[... same pattern for 46223, 46253, 46256, all 200 OK ...]
weewx_clearskies_marine.services.ww3_station_selection: select_boundary_stations: ocean --
  bearing 215.0 deg -> sides S/W -- S side: [('46223', '0.9km', '461.6m')],
  W side: [('46256', '2.0km', '114.7m'), ('46222', '13.1km', '487.9m'), ('46253', '0.1km', '291.5m')],
  rejected: 4032
```

This exact 4-station-fetch, 2-side-selection sequence repeats every cycle in the sampled window
(marine runner cadence ~7 min apart) — confirms this is steady-state production behavior, not a
one-off. Also noted in the same window (different subsystem, §(a) caveat): repeated
`surf_1d_pipeline` `bulk-falling-back` WARNINGs for many transect indices, all with the identical
bulk triple `Hs=0.43 m, Tp=6.9 s, Dir=220°` — flagged above as a candidate location for the
"⅓ energy"/"4 s everywhere" symptom, downstream of (not caused by) the WW3→SWAN L1 boundary.
`journalctl -u weewx-clearskies-marine` also showed unrelated NDBC spectral-fetch WARNINGs
(`providers/buoy/ndbc.py`, stations `prjc1`/`46253` — a buoy-observation provider, not the WW3
model boundary) — noted for completeness, not part of this pin. The service's OOM crash-loop
(mentioned in the task brief) did not visibly interrupt this window's log stream; per
instructions, not investigated further.

---

## (e) Boundary point(s) used

- **L1 domain centre:** (33.5854 N, -117.9781 W) — Huntington Beach area, consistent with the
  "HB Pier" references throughout the docs (`swan-commands-extract.md` PT* correction section,
  `docs/manuals/PROVIDER-MANUAL.md §14.15`).
- **Offshore bearing used to pick sides:** 215.0° (from the geography ray-cast's open-water
  bearing, `domains.open_water_bearing_deg`, threaded at `providers/nearshore/swan.py:2653`, NOT
  beach-facing bearing — see the G2/AD-2 comment at `swan.py:2644-2652`) → routes to sides **S**
  and **W**.
- **Stations selected, this cycle** (from the journal, §(d)):
  - Side **S**: station `46223`, 0.9 km from L1 centre, 461.6 m along-side foot-point distance.
  - Side **W**: station `46256` (2.0 km, 114.7 m), `46222` (13.1 km, 487.9 m), `46253` (0.1 km,
    291.5 m) — all three on the W side this cycle.
  - `rejected: 4032` — 4032 catalogue stations rejected (distance/depth/kd criteria), confirming
    the full NOAA station catalogue is being screened, not a hardcoded shortlist.
- **Selection logic:** `select_boundary_stations()` (`ww3_station_selection.py:1136-1296`),
  called via `select_boundary_stations_with_cycle_fallback()` from
  `providers/nearshore/swan.py:2637-2654`. Distance threshold, depth/kd-agreement checks, and the
  ≥2-total/≥1-per-side viability gate are all in this module (§(c)/(d) above).
- **`[len]` positioning on the SWAN side line:** computed in UTM meters from each side's CCW
  begin corner to the station's foot-point (`swan_formats.py:2635-2664`), NOT the raw lat/lon
  distance used only for candidate ranking/ordering (`ww3_station_selection.py`'s internal
  `len_deg` ordering key — explicitly noted as NOT usable directly for `[len]`, R5 fix,
  `swan_formats.py:2568-2586`).

---

## What I could NOT determine, and why

1. **Whether the "⅓ energy at the bar" / "0.47–0.51 m" symptom the plan cites as "today's value"
   was measured before or after accounting for the now-real L1 boundary**, i.e. whether the plan
   authors knew about T8.10c/C-87 when drafting Round Y. I found no dated measurement artifact for
   that specific number in the marine repo or in `docs/planning/scratch/` as of this commit — it
   is asserted in the plan text without an inline citation to a specific run/timestamp I could
   independently locate. Searched: `grep -rn "0.47" docs/planning/scratch` and
   `grep -rn "0.47.*0.51\|3.27 m handoff"` across `docs/` — no hits beyond the plan itself. Not
   pursued further (would require running or re-deriving a measurement, out of scope for a
   read-only fact-pin).
2. **The exact depth/kd-agreement acceptance criteria inside `_finalize_stage2()`**
   (`ww3_station_selection.py`, referenced at lines ~1172-1296) were not traced line-by-line —
   I confirmed the ≥2-station/≥1-per-side gate and the distance gate precisely, but the kd
   (depth-based wavenumber) agreement test's exact numeric tolerance was not pinned to a specific
   line. Not needed for the priority sub-question or the per-level command table; flagging so Y1
   doesn't assume it was verified here.
3. **Whether `surf_1d_pipeline.py`'s per-transect bulk-fallback (§(a) caveat) is itself new
   behavior, longstanding, or was present before T8.10c too** — I did not run `git log` on
   `surf_1d_pipeline.py` to date that code path, since it's adjacent to (not inside) the Y0 scope
   of "the boundary construction path." Flagged as a finding, not further dated.
4. **The OOM crash-loop's effect on tonight's journal**, if any — the brief said not to
   investigate; I did not check service uptime/restart timestamps around the sampled window
   beyond confirming the four fetch cycles themselves look complete and successful.

---

## Files touched by this task

Only this file was created/modified: `docs/planning/scratch/Y0-FACT-PIN-2026-08-05.md`. No code,
no other docs, no remote git operations.
