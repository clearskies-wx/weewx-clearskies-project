# Z0 FACT-PIN — 2bb1cd1 — 2026-08-06

(Produced by z0-factpin read-only agent; saved verbatim by the lead. Lead has NOT yet
independently re-verified every pin — Z1 acceptance will spot-check per the round gate.)

**Repo:** `weewx-clearskies-marine`. **HEAD pinned:** `2bb1cd195ef05839db376e6bb0b384ab9aa9dbe1`
(short `2bb1cd1`). Of the files Round Z's design cites, only `services/surf_1d_pipeline.py`
was touched by intervening X/H-1/M-0 work; `services/grid_sizing_chain.py`,
`enrichment/bathymetry.py`, and `endpoints/beach_profile.py` are byte-for-byte unchanged
since `d74c578`.

## 1. Transect selection today

- Chosen in `_compute_main_break_zone()`, `services/surf_1d_pipeline.py:1355-1541`
  (+74-line drift from plan's :1281-1467 — X rounds landed above it). Criterion (:1518-1528):
  closest face height to zone mean, ties → index-distance to zone center, then lowest index.
  ZERO bathymetric fields read — pure face-height statistic (plan premise confirmed).
- Second selector `_select_best_transect()`, `endpoints/beach_profile.py:357-377` — prefers
  `representative_transect_index` when given; else legacy max-face fallback. Called :1199-1201,
  :1263-1265.
- **Hysteresis/stickiness CONFIRMED ABSENT** (not merely unfound): both call sites (:2005,
  :3021) pass no incumbent/threshold; repo-wide grep for sticky/hysteresis/20%/SWITCH_THRESHOLD
  in selection files = zero relevant hits.
- **Naming-collision trap:** `surf_1d_analytical.py:1322-1357` `_BREAK_REARM_HYSTERESIS = 0.05`
  is a PRIOR, already-shipped, unrelated "Round Z" (2026-08-05) — single-transect break re-arm,
  legacy branch only. NOT the plan's 20% switch mechanism. Z auditors must not confuse them.
- Persistence: `representative_transect_index` is a `PipelineResult` field (:462), written
  fresh at :2104/:3108 every run, serialized via `swelltrack_cache.py:159/:324` — persisted but
  NEVER read back into the next cycle's computation = the wander mechanism, live at HEAD.
- Existing per-request override: `?transect_index=` on `get_beach_profile`
  (`beach_profile.py:890-928`) — client-supplied, single-request, does NOT touch
  `PipelineResult.representative_transect_index`; `endpoints/surf.py:1438` consumers
  unreachable by it.

## 2. Anchoring defect — CONTRADICTION with plan's cited fix site

- Plan cites `grid_sizing_chain.py:1485-1488` (COARSE-grid `find_shoreline_from_grid`) as the
  defect site. Code reality: that COARSE anchor is **deliberately overwritten at :1629-1632
  with a MEDIUM-grid recomputation** ("MEDIUM anchor supersedes COARSE" per the code's own
  comments at :1561-1565). The persisted top-level `"profile"` array (the measured 2.822 m
  depth-at-distance-0 artifact) traces to the MEDIUM anchor via raw_profile :2094-2096 →
  interpolate :2270-2272 → payload :2283. **A fix scoped only to :1485-1488 would NOT fix the
  reported defect.** Real fix surface: :1629-1632, with downstream consumers :1726 (L3 sizing),
  :1758, :1875 (study-area bbox), :2378-2380 (L4 structure grid) inheriting the last-written
  value.
- Consumer table of `coastline_by_spot`: write COARSE :1485-1488 → L2 sizing :1558-1565 (only
  consumer of COARSE, deliberate) → log :1589 → **write MEDIUM :1629-1632** → L3 :1726 →
  log :1758 → study-area :1871-1898 → profile extraction :2048-2096 → L4 :2378-2380.
- Per-transect path confirmed fine-grid-preferred (plan premise holds): :2168-2171 with
  `_anchor_grid = fine_grid if fine_grid is not None else medium_grid` (:2131). Real disparity
  = MEDIUM (shared) vs FINE-preferred (per-transect), not COARSE vs FINE as the plan frames it.
- LEAD NOTE (architectural read): fixing the anchor at the real site = the CLAUDE.md
  methodology precedent ("sizing a grid from each spot's own bearing instead of a cluster
  centroid" — the anchor's job unchanged, it now actually does it). Consumers inheriting a
  more-precise anchor is the approved Z-D2 intent; recorded for the Z gate, no new operator
  question raised.

## 3. Z-D1 hook point (optional operator pin, register item 1)

- `_compute_main_break_zone()` (single positional param) + both enclosing callers
  (`_run_pipeline_per_transect` :1601-1618, `run_pipeline` :2117-2136) have NO override slot —
  a pin needs a new param threaded through all three (:2005, :3021 call sites). NOT "no rework"
  today.
- `_select_best_transect()` already correctly shaped (optional index param, prefers it).
- Endpoint layer already proves an explicit-index path end-to-end (`?transect_index=`), but a
  persisted per-spot pin must be config-sourced and reach `_compute_main_break_zone()`'s
  callers so `endpoints/surf.py` score-card/heat-map consumers see it too.

## 4. reestablish_spot teardown inventory

- `reestablish_spot()` DOES NOT EXIST at HEAD (unbuilt Z2 work, consistent with plan). Existing
  `_clear_stale_swan_run_state(swan_work, reason)` (`grid_sizing_chain.py:400-492`, called only
  from `_cold_start_swan_if_geometry_changed` :348-397): deletes `*_hotstart*.dat`, run dirs
  `level1/ level2/ level3_* level4_* stationary/`, hash markers `level3_*_bbox_hash.txt` /
  `level4_*_geom_hash.txt` — all under `/var/run/weewx-clearskies/swan`. Explicitly does NOT
  touch `/etc/weewx-clearskies/` bathy caches (docstring :420-422). **NOT spot-scoped** (no
  spot_id param; globs everything — real gap for the per-spot routine; 5 spots live).
- Artifact enumeration (creating code pinned in fact-pin agent report):
  spot_profiles/<id>.json (per-spot; `_PROFILE_CACHE_DIR` independently defined 3x — beach_profile.py:213,
  surf.py:251, swan.py:211 — import, don't add a 4th copy); swan_grid_sizing.json (shared);
  swan_bathymetry_L1/L2.json (shared); L3_<hash>/L4_<hash>/PROFILE_<hash>.json (per-cluster/
  bbox hashes, ACCUMULATE — no deletion code anywhere); hotstart .dat (spot-scoping per-file
  unverified); run-state dirs (own artifact class — teardown spec should itemize); hash
  markers; forecast_cache.json `spots.<id>` (in-place key deletion, swan.py:1424/:1496/:1521/
  :1613); `swan-precleanup-*` dirs (**no in-repo writer found — COULD NOT VERIFY origin;
  teardown can glob-delete but can't prevent recurrence**); wind_timeline.json + incoming.json
  (**RESOLVED: GLOBAL, one shared timeline, module docstring wind_timeline_store.py:5-18,
  DORMANT — EXCLUDE from per-spot teardown**); great_lakes/<lake>.tif (shared regional raw
  cache — exclude); operator_bathymetry/operator.tif+meta (global operator INPUT — exclude,
  rebuilt spot should still consult it).

## 5. Other contradictions / verifications

- Line-anchor re-pin required at Z1 dispatch for surf_1d_pipeline.py functions (+74 drift).
- Audit Table 4 "median of 162 per-transect anchors" remains COULD NOT VERIFY from static code
  (live librewxr artifact).
- Z-D3/Z-D4 dashboard-repo anchors not re-checked (out of this repo's scope this pass).
- All other Round Z premises confirmed unchanged at 2bb1cd1.
