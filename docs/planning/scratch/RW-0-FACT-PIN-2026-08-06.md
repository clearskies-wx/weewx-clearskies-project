# RW-0 FACT-PIN (condensed) — 2bf825b — 2026-08-06

Full agent report in session transcript; this file preserves the load-bearing pins.

## 1. Real consumers of wavewatch output
- 3 call sites, all endpoints/marine.py: :217-235 (list cards — reads ONLY forecast[0].waveHeight),
  :437-452 (detail observation — same single field), :579-590 (full forecast array → Boating tab).
- Dashboard truth: ONLY 3 surfaces consume wavewatch — marine landing cards + grid teaser
  (waveHeight only) and BoatingTab (chart/table of waveHeight/wavePeriod). Surfing tab already
  reads the MODEL (/surf, multiSwell). BeachSafety = NDBC-sourced. Fishing's swell display field
  is wired but NEVER populated (fishing.py never passes swell kwargs — dead UI element).
- Marine locations = every [marine][[locations]] config entry (marine_config.py:1130-1180),
  NOT surf-only; [[[[surf]]]] sub-block optional. Locations without it (fishing/boating-only)
  have NO SWAN coverage — today their only forecast wave source is wavewatch's global grid.

## 2. Model-ingested spectra as source
- Station .spec spectra: fetched per cycle (ww3_station_selection.py:835-880), converted to
  SWAN boundary ASCII in the ephemeral run dir, then GC'd — NOTHING retained. No cache anywhere.
- NO station→spot mapping exists: stations selected per-side of the SHARED L1 domain once per
  cycle (min 2 total); a spot's wave state exists only after L1→L2→L3 propagation.
- ⇒ NAIVE "read nearest station's raw spec for the card" would DISAGREE with what SWAN computes
  for the spot (no refraction/shoaling) — a two-truth problem inside the one-truth ruling.
- .spec forecast-hour count/interval: COULD NOT VERIFY statically.

## 3. Partitioning machinery
- decompose_spectrum() (swan_spectral.py:712-896): 2-D PEAK-FINDER (not watershed), SPECOUT
  frequency-major shape; NOT production anywhere (its :906-912 comment claiming otherwise is
  STALE; newer :1315-1325 docstring is accurate). Would need transpose+wrap adaptation for WW3.
- THE production partitioner = SWAN's own Fortran watershed (Hanson & Phillips 2001) via
  TABLE PTHSIGN/PTRTP/PTDIR at POINTS → parse_table_pt_partitions() →
  watershed_partitions_to_component_format() (swan_spectral.py:1238-1302). LIVE production call
  sites: surf_1d_pipeline.py:2587 (DWR baseline), swan_runner.py:468,544,606 (per-cell).
  ⇒ THE MODEL'S OWN SWELL-1/2/3 BREAKDOWN ALREADY EXISTS IN PRODUCTION for surf spots.
- Cannot run SWAN's watershed on a bare WW3 array (Fortran-internal, needs a SWAN run).

## 4. Retirement blast radius (wavewatch.py)
- dispatch.py:45,58 registration row; 3 marine.py call sites; 3 dedicated test files (refusal/
  cycle-poll/partition-keying); golden_marine.json "source" string (orphaned fixture, no reader).
- SW-2b (nearest-water-cell fix): never committed; moot if pull dies.
- grib_processor.py is SHARED (gfs/hrrr wind) — stays regardless.
- Non-surf locations lose ALL forecast wave data if pull dies with no substitute (Boating chart
  blanks; list cards blank) — the operator fork to rule.

## 5. Risks/contradictions
- ING-1's verified no-loss finding contradicts ruling 13's "ingestion is defective" premise —
  reconciled: ingestion clean; ruling 13's ONE-TRUTH goal stands on its own.
- Register order: item 13's "ALL WORK STOPPED" text predates 11(b)'s later full-go (lifted).
- swell2/3 wire fields (SW-2) currently have zero dashboard readers (types only).
