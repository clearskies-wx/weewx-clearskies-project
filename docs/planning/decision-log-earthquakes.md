# Decision Log — Earthquakes Domain
Detailed round entries. See CLEAR-SKIES-PLAN.md for the one-line index.

Covers 3b round 13: USGS, GeoNet, EMSC, ReNaSS — all 4 day-1 providers in one round (all keyless GeoJSON per ADR-040).

---
- **2026-05-11** — Phase 2 task 3b round 13 closed. **Earthquakes domain opened** per [ADR-040](../decisions/ADR-040-earthquake-providers.md) — all 4 day-1 providers landed in one round (usgs, geonet, emsc, renass — all keyless GeoJSON, single-source-per-deploy). NEW canonical entity `EarthquakeRecord` + `/earthquakes` endpoint + `epoch_ms_to_utc_iso8601` helper (USGS uses epoch ms). Brief: [`phase-2-task-3b-13-earthquakes-brief.md`](briefs/phase-2-task-3b-13-earthquakes-brief.md). Pre-round verification corrected TWO factual errors in the resume prompt: (1) the resume prompt's "dual-provider-per-region with USGS fallback dispatch" claim contradicted ADR-040 §"Single source per deploy" (which explicitly mandates single-provider-per-deploy, same shape as alerts/AQI) — surfaced to user; user picked Option A (all 4 providers in one round); (2) the resume prompt's "102 pre-existing failures parking-lot" claim was FALSE — lead full-suite verify on weather-dev at HEAD `617c185` returned **0 failed / 1766 passed / 311 skipped**. The 3b-12 rule extension (lead-pytest-verify) caught this. **Cross-check rule highest yield to date — 6 canonical mismatches surfaced for ReNaSS + GeoNet** before brief draft: (a) GeoNet `mmi` cell said `properties.MMI` (uppercase) but live response has `properties.mmi` (lowercase); (b) ReNaSS endpoint `https://renass.unistra.fr/fdsnws/event/1/query` returns 404 since EPOS-France migration — new URL is `https://api.franceseisme.fr/fdsnws/event/1/query`; (c) ReNaSS `description` is `{fr,en}` object now, not string; (d) ReNaSS `url` is `{fr,en}` object now, not string; (e) ReNaSS `magnitudeType` is populated as `properties.magType` (camelCase), not "(often null)"; (f) ReNaSS `status` derives from boolean `properties.automatic`, no `properties.status` exists. All amended in `canonical-data-model.md §4.4`, `ADR-040 References`, `EARTHQUAKE-PROVIDER-RESEARCH.md`. Per user direction 2026-05-11, ADR-040 stays Accepted (URL-only fix, no decision content change — departs from the standard "edits flip status to Proposed" workflow per explicit user authorization). 4 NEW api-docs files written from live captures today: `docs/reference/api-docs/{usgs,geonet,emsc,renass}.md`. User-resolved Q1 (capability strings: usgs="global", geonet="nz", emsc="global, primary in eu+mediterranean", renass="fr") and Q2 (cache TTL 60s for all four) baked in pre-spawn. Teammate work: api-dev landed models+infra (`86f6c8e`) → 4 provider modules (`77945e8`) → endpoint+wiring (`e8fbd28`) → 4 fix-commits responding to test-author bug surface (`67fc056`/`1b16306`/`4086477`/`5c33477`). test-author landed fixtures+unit tests (`7a677a7`) + integration+endpoint tests (`5cf9d3b`). Pytest at `5c33477`: lead-verified 1954/364/0 (498s on weather-dev). **Three lead-direct fixes against teammate work, all in commit `42843c0` (13 files, +148/-212):** (1) **cache.py extension reverted (BAD CODE).** api-dev added `_RedisCache` class + `_cache_instance` test-injection sentinel + two-tier `get_cache()` to support test-author's invented fakeredis-injection pattern. test-author's claim "follows existing IQAir test pattern" was FALSE — verified zero references to `_RedisCache`/`_cache_instance` in IQAir tests. The established pattern at `tests/test_providers_alerts_unit.py:660` (`object.__new__(RedisCache); redis_cache._client = fake_redis`) was already there. Production cache.py reverted to pre-3b-13 state; all 8 test files (4 unit + 4 integration) rewritten to the established pattern. Process violation: api-dev should have STOPPED and pinged the lead before extending a production cache module outside brief scope; instead shipped extension and flagged in closeout. (2) **Naming aliases removed.** Brief prescribed `_<Provider>EventFeature`/`_<Provider>Response` mixed-case; test-author wrote all-caps prefixes; api-dev added 7 alias lines as workaround. Removed aliases; renamed ~62 test references via `replace_all`. (3) **`_to_canonical(feature, raw_feature)` shape locked.** api-dev added shape-detection workaround `raw_props = raw_feature.get("properties", raw_feature)` to handle both full-feature-dict and properties-dict callers; verified test callers all pass full feature dict — fallback was dead code. Replaced with `raw_props = raw_feature["properties"]`. Pytest at `42843c0`: lead-verified 1954/364/0 (492s). Auditor returned 2 findings, both lead-direct in `ecd7e75`: F1 MED `EarthquakesQueryParams.from_/to` declared as `str | None` with no validator, brief mandated parseable-ISO validation, security-baseline §3.5 trust-boundary validation bypassed → changed to `datetime | None` matching the in-file precedent (`ArchiveQueryParams`, `AQIHistoryQueryParams`); provider `fetch()` signatures now take `datetime | None`; each provider formats with `.isoformat()` at upstream URL boundary. F2 LOW `try/except IndexError → raw_feature = {}` fallback in 4 fetch loops was dead defensive code (trigger structurally impossible since `wire.features` and `raw_features` come from the same `raw_json`) AND when fired made the failure WORSE (KeyError → 500 instead of 502 ProviderProtocolError) → replaced with `zip(strict=True)` wrapped to ProviderProtocolError. 5 files changed, +94/-65. No process notes from the auditor. **Process violations from this round (decision-log only per user's durable direction; carried forward to 3b-14 spawn-prompt constraints):** (a) Lead idle-waiting on background tasks was the dominant token-burn pattern; user explicit direction "WHY WERE YOU SITTING THERE DOING NOTHING?" — added rule extension to 3b-14 resume prompt: lead works in parallel with background tasks. (b) Brief was overweight at 700+ lines for a 4-keyless-provider domain opener sharing one precedent; carried to 3b-14 resume prompt as "audit brief weight at draft time." (c) api-dev expanded production module outside brief scope without lead approval. (d) test-author cited non-existent precedent that lead-grep disproved in 5 seconds.
