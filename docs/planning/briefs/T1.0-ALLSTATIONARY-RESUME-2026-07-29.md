# Resume prompt — Track A / T1.0 all-stationary implementation (2026-07-29)

Paste the block below into a new session to continue. It points at the authoritative docs rather than
restating them (read the source, don't work from paraphrase).

---

You are the **coordinator (Opus)** resuming execution of **TRACK A** of
`docs/planning/MARINE-WORKING-MODEL-PLAN.md`. Delegate implementation to Sonnet agents; do the
orchestration, QC, and decisions yourself. Adversarial QC/QA (blind auditor) is MANDATORY at each gate.

**Read first, before acting (in this order):**
1. `CLAUDE.md` (architectural HARD BLOCK), `rules/coordinator.md`, `rules/agents.md`, `rules/verification.md`.
2. `docs/planning/MARINE-WORKING-MODEL-PLAN.md` — the "## Execution status — LIVE" section (top), the
   Operator decisions ledger entry "2026-07-29 — SWAN propagation scheme & time-integration" (the
   all-stationary decision + hybrid fallback + Fable-verified detail), and the `#### T1.0` task (your
   contract).
3. `docs/planning/MARINE-WORKING-MODEL-CONCERNS.md` (TA-C04, TA-C06, TA-C07 esp.).
4. `docs/ARCHITECTURE.md` — the "⚓ MARINE HANDOFF MODEL" callout (SWAN never models the break; hands off
   before the break; L4/L3/L2 rule). `reference/clearskies-dev.md` (SWAN docs are LOCAL — never download;
   `pdftotext -layout docs/reference/swan-user-manual.pdf`). The prior coordinator's working notes are in
   the session scratchpad `TRACK-A-PROGRESS.md` if available.

**Where things stand (verify, don't assume):**
- **Phase 0 COMPLETE + deployed** (marine repo commits `9f238ba` T0.1, `63c6191` T0.4, `94c43f4` T0.3,
  `f657bda` T0.5, `76ced7f`+`d488449`+`f39f9cf`+`c15944d` gate re-audit/guards; T0.2 in meta repo). QC
  Gate 0 passed two blind adversarial passes. Marine repo HEAD = origin/main = `c15944d`.
- **T1.1 OBSTACLE wrap DONE** (`a915deb`). D1 fixed.
- **A real cold run was attempted and failed HONESTLY at L1** — the hardened gate caught a CFL/scheme
  error (nonstationary S&L scheme). This is the real M1 blocker, not D1. The "217 s complete" runs before
  it were fake (vacuous gate passed non-computing runs). No real run-time baseline exists yet.
- **DECISION (operator): T1.0 = ALL-STATIONARY** (quasi-stationary at all 4 levels) + directional
  resolution 36→72 + `NUMERIC alfa` under-relaxation ON + hourly cadence + mandatory COLDSTART on deploy.
  HYBRID (L1/L2 nonstationary) is RETAINED in the plan as a documented fallback — do NOT delete it or T0.3.
- **librewxr service is STOPPED** (dev), trace drop-in reverted (prod trace off). Deploy commit `c15944d`
  is what's installed. Forensics preserved (`M1FAIL_level1_PRINT.txt`;
  `librewxr:/var/log/weewx-clearskies/marine-trace-20260729.jsonl`).

**NEXT ACTION — dispatch T1.0 per `#### T1.0`:**
1. Pre-flight the marine repo (clean, HEAD=origin/main). Dispatch **`clearskies-api-dev`** to implement the
   all-stationary emitter + caller per the `#### T1.0` contract. THE KEY DESIGN POINT (Fable finding 1): the
   single `stationary` flag in `build_swan_input` today gates EIGHT emissions (INPGRID WIND/WLEVEL/CURRENT
   NONSTAT clauses + 4 OUTPUT clauses + COMPUTE). All-stationary is a THIRD emission mode = `MODE
   NONSTATIONARY` + **time-varying** NONSTAT inputs + per-hour OUTPUT clauses + a **sequence of
   `COMPUTE STAT [t]`** (one per forecast hour) + HOTFILE + `NUMERIC alfa=0.01`. **TRAP: do NOT emit the
   COMPUTE-STAT loop while dropping the NONSTAT inputs/OUTPUT clauses — that freezes one weather snapshot
   across 72 h with no error.** Plus `CIRCLE 36→72` at `swan_formats.py:1529`. Require a scope-ack
   (esp. the `compute_mode` enum vs `stationary_sequence` flag representation).
2. Then `clearskies-test-author` (or api-dev, per the T0.3 precedent) for the emission guard test.
3. Then **coordinator: deploy with COLDSTART** (`scripts/deploy-marine.sh`, then clear `*_hotstart.dat`
   — 36→72 invalidates hotstarts and the F1 guard won't catch it), run one cold full cycle, and MEASURE:
   wall-clock (first real baseline), per-iteration convergence curve (to set the `mxitst` cap — currently
   default 50; don't guess it low), and the T0.5 trace (does the swell survive to the handoff). This is M1
   evidence → QC Gate 1 (blind auditor).

**Standing rules for this work:**
- Architectural HARD BLOCK: all-stationary + 36→72 + alfa are operator-APPROVED. Anything BEYOND (change a
  coefficient/`_OBSTACLE_PARAMS`, switch to BSBT, move a grid boundary/handoff, change the model's domain)
  STOP and ask. Coordinator self-check every instruction against the 7 triggers.
- Push/deploy: the operator authorized push/deploy for testing this session; re-confirm if unsure. Never
  push without intent. Agents never push/pull/fetch/rebase/merge; local checkout only; never edit/commit on
  containers.
- Independently re-run every agent claim (paste raw output); a run counts only if the service
  `ExecMainStartTimestamp` postdates the deploy of the commit under test (record commit + start-time).
- Validate physics against REALITY (Surfline face 4–6 ft; three trains 18 s@195°/16 s@168°/10 s@184°),
  never the model's own output. Pick the comparison quantity before looking.
- SWAN docs are LOCAL — never download.
- Carry-forward concerns: TA-C06 (add a POSITIVE completion-marker gate check, validated against T1.2's
  first real PRINT — closes the negative-scan gate residual); TA-C04 (read the trace via the full matrix /
  `energy_by_freq`, not just the 3-band summary — 18 s & 16 s both land in the "long" band); TA-C05
  (add spec_l3_nestout trace only if the collapse localizes between l2_nestout and the handoff).

---
