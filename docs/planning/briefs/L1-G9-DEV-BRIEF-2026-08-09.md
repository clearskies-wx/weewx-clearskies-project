# ROUND BRIEF — G9: L1 box-envelope clamp (L1-BOUNDARY-REBUILD-PLAN)

**Round identity:** Task G9 (operator-ruled 2026-08-09, Q6: the 100 km cap binds the L1
BOX per axis, not each scan ray). Lead: coordinator. Dev: `clearskies-api-dev` (Sonnet).
Test: `clearskies-test-author` (KATs h/i, separate brief/dispatch, same round). QC: rides
the G-Accept re-run (Gate G already passed; G9 is the operator-ruled correction).
**Repo:** `c:\CODE\weather-belchertown\repos\weewx-clearskies-marine` (local commits only).
Dispatches when the S2/S3 round closes (marine repo runs one round at a time).

**Authorization:** G9 IS architectural (trigger 3 — L1 extent) and is OPERATOR-RULED
2026-08-09 (plan Q6 closure + §G9 design; ADR-104 D2 amendment). Anything beyond §G9's
text → STOP and surface.

## READING LIST (read BEFORE any code)
1. Plan `docs/planning/L1-BOUNDARY-REBUILD-PLAN-2026-08-08.md` §G9 (lines ~557-582) —
   the full decided design, verbatim; and the Q6 closure block (§OPEN OPERATOR QUESTIONS)
   for the ruling's reasoning.
2. `docs/archive/decisions/ADR-104-*.md` — D2 amendment (cap semantics) + D11 (best-
   achievable, silent).
3. `weewx_clearskies_marine/services/swan_domain.py` — `_compute_level1` (:1176-1403).
   The clamp lands AFTER the final min/max envelope + margins (:1391-1397), BEFORE the
   `GridDomain` return (:1399). Read the whole function including the G3 enclosure loop
   (:1359-1389) and the override path (:1287).
4. `weewx_clearskies_marine/services/boundary_reconstruction.py` `_offshore_sides()`
   (:187) — the open-water-side semantics that decide WHICH edge per axis is the
   offshore edge (the one that pulls in). Import/reuse; do not duplicate the logic.
5. `weewx_clearskies_marine/services/geography.py` — `L1_MAX_EXTENT_KM` (:124) and its
   docstring (:21, :188-199): semantics become "max L1 box span per axis".
6. `tests/test_island_autosizing.py` — the row-(e) box literals you are AUTHORIZED to
   update same-commit (this task's operator ruling supersedes the pinned literals; the
   stale-test rule's same-commit clause applies).

## PRE-ROUND VERIFICATION (lead, 2026-08-09)
Anchors above verified at marine HEAD `eecfabc`. If the S2/S3 round moved HEAD before
your dispatch, the lead re-states HEAD at dispatch; S2/S3 touches providers/ocean,
providers/tides, providers/nearshore/swan.py (tide site), services/swan_runner.py,
services/vertical_datum.py — NOT swan_domain/geography — so drift should be zero;
re-verify and report in scope-ack.

## SCOPE
**Modify:** `services/swan_domain.py` (`_compute_level1` — the envelope clamp only),
`services/geography.py` (L1_MAX_EXTENT_KM docstring only — value unchanged),
`tests/test_island_autosizing.py` (row-(e)/existing literals to the capped box ONLY —
new KATs h/i belong to test-author, do not write them).
**Do NOT touch:** the per-ray/per-enclosure cap applications inside `_compute_level1`
(G3's prefilter stays as-is — the box clamp is a FINAL authority added after them, not a
replacement), the near-lee clamp, `_compute_level2`+, the override key semantics
(override > cap still refused — verify the existing refusal covers the new semantics and
report if not), boundary_reconstruction.py, grid_sizing_chain.py, any provider, any
other test file.

## DESIGN (decided — plan §G9 verbatim; hard edges)
- After the full envelope (base + enclosures + near-lee + margins, :1394-1397):
  per-axis `span ≤ L1_MAX_EXTENT_KM`. Exceeded → pull in ONLY that axis's offshore edge
  (per `_offshore_sides` on the open-water bearing). The coast-side edge (spots bbox +
  landward margin + lateral margins) NEVER moves and is the floor; floor itself > cap →
  loud config-push refusal naming both numbers (cannot happen at HB).
- Enclosure/wrap points left outside the clamped box are simply cut (D11
  best-achievable, SILENT — no warning spam).
- Km↔degree conversion for the span test must match the function's own
  `km_per_deg_lat/lon` arithmetic (no new geodesy).
- Expected HB outcome (accept target, do not hardcode): ~91×100 box, S edge ≈33.18
  (north of SCI 33.03 tip), Catalina S shore ≈33.30 inside.

## VERIFICATION (yours, before closeout)
`.venv-round4\Scripts\python.exe -m pytest tests/test_island_autosizing.py tests/services/ -q`
from the marine repo root; record pre/post counts (baseline at `eecfabc`: 210 pass /
3 tracked pre-existing fail). List every literal you updated and why.

## MANDATORY BLOCKS
Comply verbatim with the three blocks (git restrictions; stale-test; architectural) in
`docs/planning/briefs/L1-PHASE-W-DEV-BRIEF-2026-08-08.md` §MANDATORY BLOCKS.
**SCOPE-ACK REQUIRED via SendMessage to "main" before any code:** deliverables,
exclusions, exact pytest selection, anchor re-verification. Wait for confirmation.
Tone: concise.
