# S8.1 pre-flight brief — fine DEM source + sample density for the WW3 G1 transparency field (READ-ONLY)

**Round identity:** MARINE-AND-MAPS-PLAN Phase S, task S8.1 pre-flight. Date 2026-08-27. Lead:
coordinator. Implementer: one general-purpose agent (Sonnet), read-only. Deliverable — the ONLY
file you write: `c:\CODE\weather-belchertown\scratch\S8.1-PREFLIGHT.md`. Scratch data you download
goes under `c:\CODE\weather-belchertown\scratch\s81-preflight\` only (delete anything > 50 MB when
its numbers are captured).

## Questions (answer each with the command/URL and raw evidence)
1. **The G1 box.** Read it live: `ssh -F .local/ssh/config librewxr "sudo -n cat /etc/weewx-clearskies/swan_grid_sizing.json"`
   (from `c:\CODE\weather-belchertown`) → the `level1` block corners and `ww3_leg` block (nx, ny,
   resolution). Paste them.
2. **What fine DEM fully covers that box?** (a) Check the shipped index
   `repos/weewx-clearskies-marine/weewx_clearskies_marine/data/ncei_regional_dem_index.json` for any
   entry fully containing the box (the `find_best_dem()` rule, `services/bathymetry_resolver.py:180–229`).
   (b) If none: identify the NOAA NCEI Coastal Relief Model volume for Southern California (3 arc-second,
   ~90 m) and whether it is served by NCEI THREDDS/OPeNDAP (the `_OPENDAP_BASE_URL` pattern in
   `bathymetry_resolver.py`) or only as a download; record the exact dataset name, URL, vertical datum,
   horizontal datum, resolution, and licence/terms. (c) As the fallback of record: ETOPO 2022 15″ via the
   existing `download_swan_depth_grid()` path — state its samples-per-G1-cell (≈ 460 m samples in a
   1 km cell ≈ 4–5). Web searching NOAA/NCEI dataset pages is allowed for THIS question only (not for
   WW3 behaviour — the WW3 manual is local, PRIME DIRECTIVE 10).
3. **Sample density.** For the chosen fine source: samples per 1 km G1 cell and the resulting
   quantisation step of the open-water fraction `f` (1/N). State whether KAT (b)'s "< 2 % of a
   synthetic island's area" (plan §S8.1 item 4b) is meetable, and if not what tolerance the source
   supports — derived, with the arithmetic.
4. **Island cells today.** With the live L1 ETOPO cache (`sudo -n cat /etc/weewx-clearskies/swan_bathymetry_L1.json`
   — it is large; stream it through `python3 -c` on the host and print only counts), reproduce the
   current nearest-sample mask over the G1 grid (the exact `_ww3_nearest_source_depth` rule,
   `services/swan_domain.py:3153–3179`, and the grid dims rule `:3130–3150`): count dry cells, wet
   cells, and — using a 3×3 sub-sample of the ETOPO cache inside each cell — how many cells are
   "partial" (some samples dry, some wet). Paste the counts.
5. **WW3 transparency grammar.** From the LOCAL manual only: `docs/reference/ww3-user-manual-v6.07.txt`
   §3.4.7 (`:6722–6793`), `:15349–15363`, `:15927–16018`, and `docs/reference/ww3-commands-extract.md`
   (SYNTAX row 6a): quote the exact `ww3_grid.inp` lines for `FLAGTR = 2` and the obstruction
   field read line (unit, scale factor, IDLA, IDFM, format, NAME), and the array ordering/IDLA
   convention the depth block uses so the writer can mirror it. Also quote the "transparency at cell
   centres" semantics (inflow 0.5(1+τ), product τ).
6. **Existing writers?** Confirm (grep) that nothing in the marine package writes `G1_bottom.txt` /
   `G1_status.txt`; list what `build_ww3_grid_deck()` (`swan_domain.py:3439–3506`) references and
   what `WW3Runner.run_grid()` (`services/ww3_runner.py:326–350`) expects as `input_files`.

## Rules
Read-only: no repo edits, no git add/commit/push/pull/fetch/rebase/merge/checkout-of-remote, no
host writes anywhere, no deploys. `sudo -n` read-only on librewxr is allowed for the two files
named. Never run the full pytest suite (you run no tests). Do not web-fetch SWAN or WW3
documentation (local copies are authoritative). Plain, terse deliverable; every number with its
command.

## Reporting
Scope ack via SendMessage first, then proceed (pre-confirmed). Closeout: the deliverable path and
the answers to 2, 3 and 4 in five lines.
